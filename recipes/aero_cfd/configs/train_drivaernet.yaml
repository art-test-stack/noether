# @package _global_

# Define key values here that are used multiple times in the config files.
dataset_root:  <path to your drivaernet dataset root>
dataset_kind: noether.data.datasets.cfd.DrivAerNetDataset

defaults:
  - train_caeml 

trainer:
  max_epochs: 50