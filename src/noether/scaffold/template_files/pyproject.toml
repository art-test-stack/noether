[build-system]
requires = ["setuptools>=76.1.0"]
build-backend = "setuptools.build_meta"

[project]
name = "__PROJECT__"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "emmiai-noether",
]
